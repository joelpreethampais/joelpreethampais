variable "allow_rules" {
  type = list(object({
    protocol = string
    ports    = list(string)
  }))
  default = [
    {
      protocol = "tcp"
      ports    = ["22", "80", "443"]
    },
    {
      protocol = "udp"
      ports    = ["53"]
    }
  ]
}
